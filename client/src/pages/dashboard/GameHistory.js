import { SearchContainer, HistoriesContainer } from "../../components";

const GameHistory = () => {
  return (
    <div>
      <SearchContainer />
      <HistoriesContainer />
    </div>
  );
};

export default GameHistory;
